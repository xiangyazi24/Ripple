# Doty Exact-Majority — Phase-F-prep Independent Audit (2026-06-10)

**Auditor:** independent audit agent (READ-ONLY on all `.lean`; axiom checks via `#print axioms`
on temp imports against prebuilt oleans; NO `lake build`).
**Scope:** the 25 campaign output files in `Probability/` produced/edited in the last 48h (see list
below). Mid-edit files `Phase5Convergence`/`ReserveSampling` verified `working tree == HEAD` (so the
working tree IS the committed state — audited directly).
**Method:** (1) raw grep for `sorry`/`axiom`/`native_decide`/`admit`/`sorryAx`/`unsafe`/`partial`
across all 25; (2) `#print axioms` on 16 headline theorems across the 11 files whose oleans are
prebuilt-fresh; (3) headline-hypothesis classification by reading each capstone; (4) event-shape
sanity on the 5 named capstones; (5) cross-file feeder consistency; (6) campaign DONE-claim
spot-checks (12 claims).

---

## 1. Axiom / sorry hygiene — verdict: CLEAN

Raw grep over all 25 scope files for `sorry`, `axiom `, `native_decide`, `admit`, `sorryAx`,
`unsafe`, `@[implemented_by]`, `@[extern]`, `partial def`: **ZERO hits in all 25 files** (excluding
none — there are no doc-comment occurrences to exclude either). No proof-bypass constructs anywhere.

`#print axioms` spot-check on 16 headline theorems (the 11 fresh-olean scope files) — **every one**
returns exactly `[propext, Classical.choice, Quot.sound]` (the standard Mathlib axiom set;
axiom-clean):

| theorem | file | axioms |
|---|---|---|
| `DotyParams.goodFrontWidth_whp_final` | DotyParams | clean |
| `EarlyDripMarked.hB_discharge` | EarlyDripMarked | clean |
| `EarlyDripMarked.perWindowDelta_uniform` | EarlyDripMarked | clean |
| `EarlyDripMarked.goodFrontWidth_whp_at` | EarlyDripMarked (WidthPrefix ns) | clean |
| `ClockBudgets.clock_unconditional_concrete` | ClockBudgets | clean |
| `ClockWeakAssembly.clock_real_faithful_O_log_n_W` | ClockWeakAssembly | clean |
| `ClockFrontSyncFromWidth.frontSync_whp_of_goodFrontWidth` | ClockFrontSyncFromWidth | clean |
| `GatedDrift.gated_real_tail_full` | GatedEscape | clean |
| `GatedDrift.kill_now_escape_le_prefix_union` | GatedKillNow | clean |
| `KernelWindowDrift.kernel_windowDrift_tail` | KernelWindowDrift | clean |
| `Phase10Drop.phase10_expected_stabilization'` | Phase10ExpectedTime | clean |
| `Phase10Drop.phase10_expected_stabilization_O_nsq_log` | Phase10ExpectedTime | clean |
| `expectedHitting_split_geometric` | ExpectedHitting | clean |
| `expectedHitting_geometric` | ExpectedHitting | clean |
| `OneSidedCancel.levels_union_tail` | OneSidedCancel | clean |
| `OneSidedCancel.crude_PhaseConvergenceW` | OneSidedCancel | clean |
| `PhaseConvergence.composeW_two_phases` | PhaseConvergenceWeak | clean |

**Axiom spot-checks NOT performed** (oleans not in the local cache — recently edited, final remote
build pending; source-grep hygiene is clean for all of them): `WidthPrefixConcrete`,
`ConditionalPhaseProgress`, `RoleSplitConcentration`, `CounterTimeout`, `Phase4Convergence`,
`Phase5Convergence` (stale), `Phase7Convergence` (stale), `Phase8Convergence`, `Phase10Convergence`
(stale). Their headline theorems are simple reductions to the engine lemmas already axiom-checked
(e.g. `phase0_stage1_whp → real_bad_le_janson_add_escape`; `phase7Convergence'' →
crude_PhaseConvergenceW`; `phase4Convergence` self-contained), so axiom-cleanliness is inherited
modulo a final compile. Recommend a confirming `#print axioms` pass on these after the next remote
`lake build`.

---

## 2. `True := trivial` status markers — verdict: HONEST (2 in scope, both correct)

The campaign-wide `_status : True := trivial` convention appears in TWO scope files:

- `ClockUnconditional.lean:431  clock_unconditional_status : True := trivial`
- `ClockWeakAssembly.lean:593  clock_weak_assembly_status : True := trivial`

Both sit under a `/-! ## Status -/` doc-comment header, AFTER the file's real content theorems
(`sidePrefix_le`, `clock_real_faithful_O_log_n_W`), are named `*_status`, and assert nothing about
the protocol. These are HONEST STATUS anchors (the same pattern as the out-of-scope
`clock_front_shape_status` the brief named). They do NOT stand in for content. The remaining ~20
`*_status` markers live in OUT-OF-SCOPE earlier front-profile files (ClockFrontShape, ClockEnvMaint,
ClockCapRecur, FrontSyncConc, …) and are likewise honest.

---

## 3. Per-file headline classification

Hypothesis-class legend: **(a)** protocol-structural (card/start/scale — fine);
**(b)** discharged elsewhere (named); **(c)** genuinely-open carried residual (named below in §6);
**(d)** suspicious ∀-over-class shape (flagged).

| file | headline(s) | sorry/axiom | residual class |
|---|---|---|---|
| EarlyDripMarked | `hB_discharge`, `perWindowDelta_uniform`, `floor_discharge`, `slice_discharge` | clean | (a) scales + (b) feeder math; closed building blocks |
| DotyParams | `goodFrontWidth_whp_final` | clean | (a) `hn hcard hge3 hnotP3 hclean Tcap`; **(d)** `heB`/`htB`/`hclimbB` = ∀-`T<Tcap` measure bounds (the gated-engine eB/tB + climb feeders) — documented, bound scales with them |
| WidthPrefix | `goodFrontWidth_whp_at`, `windowedFrontProfile_whp_prefix` | clean | (a) + carried `δRem`/window inputs (the B-8 prefix machinery) |
| WidthPrefixConcrete | `clock_unconditional_final`, `sidePrefix_concrete_width` | clean(grep) | (c) `εside` = `sideEps εQ εfloor (εWAt) εP εB εge3 εno3 εcpos εsucc`: εW discharged concretely (εWAt), 8 feeders carried |
| ClockFrontSyncFromWidth | `frontSync_whp_of_goodFrontWidth`, `capFeederEmpty_whp_…` | clean | (b) deterministic glue; retires the FALSE `hwin_all` (doc) |
| PhaseConvergenceWeak | `composeW_two_phases`, `composeW_n_phases` | clean | generic composition combinator; no protocol content |
| GatedEscape | `gated_real_tail_full`, `kill_escape_le_prefix_union` | clean | generic (carry `q`/`r`/`hstep`); engine layer |
| GatedKillNow | `kill_now_escape_le_prefix_union`, `real_le_killed_now` | clean | generic K↔killK coupling; carry `S`/`q`/`hstep` |
| KernelWindowDrift | `kernel_windowDrift_tail`, `kernelWindowDrift_PhaseConvergenceW` | clean | generic Markov-kernel drift; no protocol content |
| ClockKilledMinute | `clock_killed_seed/bulk_stepW`, `clock_real_seed_step_gated` | clean | (a)+(b) killed-minute brick |
| ClockWeakAssembly | `clock_real_faithful_O_log_n_W` | clean | (c) `q`/`hstep` (per-step gate-escape rate, the §6 drip bound) |
| ClockUnconditional | `sidePrefix_le`, `Sgood_compl_subset` | clean | (b) the εQ/εfloor/εsync/εphase union split; carries named feeders |
| ClockBudgets | `clock_unconditional_concrete`, `widthFail_concrete` | clean | (c) single open input `εside` (uniform per-τ side budget) |
| ExpectedHitting | `expectedHitting_split_geometric`, `…_geometric`, `bad_block_geometric_on` | clean | generic hitting-time toolkit; carry `q`/`δ`/absorption |
| Phase10ExpectedTime | `phase10_expected_stabilization'` (+`_O_nsq_log`, `_tie`) | clean | **(a) ONLY** `hn`+`S1`/`Tie` start — NO probabilistic residual; fully closed `O(n² log n)` |
| ConditionalPhaseProgress | `timed_phase_progress_real_{tinyClock,bigClock}` | clean(grep) | **(d)** `hfloor : ∀ b, AllClockGEpCard p n b → mC ≤ posClockCount p b` — Lemma-5.2 clock floor (the hwin_all-shape); documented as E4-supplied |
| RoleSplitConcentration | `phase0_stage1_whp` | clean(grep) | (c) free `S` + `q`, `hstep : ∀ x∈floorGate, x∈S → K x floorGateᶜ ≤ q` (Lemma-5.1 Chernoff numbers) |
| CounterTimeout | `counterTimeout_PhaseConvergenceW(_perStep)`, `counterTimeout_chain` | clean | generic; carry `q`/`hstep` per-step timeout rate |
| OneSidedCancel | `crude_PhaseConvergenceW`, `levels_PhaseConvergenceW`, `levels_union_tail` | clean | generic cancellation engine; carry `q`/`hstep` |
| Phase4Convergence | `phase4Convergence`, `phase4NonTieConvergence` | clean(grep) | **(a)+explicit-rate**: NO carried `q` — rate is closed form in `hε`; convergence proved internal |
| Phase5Convergence | `phase5Convergence` | clean(grep) | (c) THREE carried: `hClosed` (InvClosed) + `q`/`hstep` (unsampledReserveU drain) + `εConc`/`hConc` (sampled-class concentration) — most residual-laden (mid-relay C-5) |
| ReserveSampling | predicates + per-pair non-increase | clean | (b) structural reserve-sampling lemmas |
| Phase7Convergence | `phase7Convergence''` (def), `phase7_three_window`, `…_post_noMinority` | clean(grep) | (c) ONLY `hstep` (σ-class-MASS drain `q`); `hClosed`+`hmono` PROVED INTERNAL |
| Phase8Convergence | `phase8Convergence` (def), `minorityU_hdrop_of_floor` | clean(grep) | (c) ONLY `hstep` (minorityU drain `q`); `hClosed`+`hmono` PROVED INTERNAL |
| Phase10Convergence | `phase10Convergence_{maj,tie,combined}` | clean(grep) | **(a) ONLY** `hn`+block-length `hsB`+`S1`/`Tie` start — NO carried `q`; whp rate discharged INTERNALLY off the closed expectation |

---

## 4. Event-shape sanity on the 5 named capstones — verdict: NO vacuous truth

- **`clock_unconditional_final`** (WidthPrefixConcrete): conclusion bounds total minute-failure mass
  by `εclock L K tbulk εbulk εside`. RHS scales monotonically with the carried `εside`; if `εside=⊤`
  the bound is `⊤` (trivial-but-honest), it does NOT collapse the LHS. The failure event
  `{¬ BulkPost (i+1)}` is genuinely satisfiable. No vacuity.
- **`goodFrontWidth_whp_final`** (DotyParams): RHS `Tcap·(KK·deltaB + (eB+tB)) + climbB` scales with
  carried `eB/tB/climbB`. Width-failure event non-empty-satisfiable. No vacuity.
- **`phase10_expected_stabilization'`** (Phase10ExpectedTime): `E[hit {wrongACount<1}]` with NO
  carried inputs; bound `3·n(n−1)(1+log…)`. The hitting target is a real absorbing set. Closed. Sound.
- **`phase0_stage1_whp`** (RoleSplitConcentration): RHS `Janson-tail + t·q + ∑_τ Kᵗc₀Sᶜ`. Edge check:
  if `S=∅`, `hstep` is vacuously satisfied BUT then `Sᶜ=univ` so `∑_τ Kᵗc₀univ = t` and the RHS GROWS
  (does not collapse). So the free `S`/`q` cannot be exploited for a vacuous bound. Event
  `{¬ roleSplitGoodMile}` satisfiable. Sound.
- **`phase7Convergence''`** (Phase7Convergence, a `def` producing a `PhaseConvergenceW`): conclusion
  mass `Kᵗ x₀ {¬(Inv7Sum ∧ classMassN=0)} ≤ ε`, `ε ≥ q^t`. If `q=1` then `ε≥1` (trivial-but-honest);
  genuine `q<1` from `hstep` gives a real bound. Event satisfiable (any positive-minority config).
  Sound. **Note**: `hClosed` and `hmono` are PROVED INTERNAL here (`invClosed_Inv7Sum`,
  `potNonincrOn_classMassN`) — the gap-2 minorityU-rise obstruction is genuinely resolved by moving
  to the σ-class MASS potential; only the drain `hstep` is carried. Strong.

---

## 5. Cross-file feeder consistency — verdict: CONSISTENT

- `ClockBudgets.sideEps` takes exactly 9 feeders `(εQ εfloor εW εP εB εge3 εno3 εcpos εsucc)`.
  `WidthPrefixConcrete.sidePrefix_concrete_width` produces
  `sideEps εQ εfloor (εWAt …) εP εB εge3 εno3 εcpos εsucc` — εW substituted by the concrete free-τ
  family `εWAt`, the other 8 carried. MATCHES campaign §B-13d. No arity/name mismatch.
- `DotyParams.goodFrontWidth_whp_final` carries `heB`/`htB` (hour-escape mass + taint tail) feeding
  `EarlyDripMarked.hB_discharge`/`front_tail_sum_le`'s `eB`/`tB` slots. Consistent.
- The FALSE `hwin_all`/`hfeeder_all` carried windows are RETIRED: no scope file consumes them as
  hypotheses (the only two mentions — DotyParams:416, ClockFrontSyncFromWidth:9 — are doc-comments
  describing the replacement). Phase B's central deliverable (drop the false `hwin_all`) is realized
  in the scope files.

---

## 6. CONSOLIDATED genuinely-open carried inputs (the honest Phase-D/F TODO surface)

All of the following are documented in `DOTY_POST63_CAMPAIGN.md` (verified — none undocumented):

**Clock chain (Phase B residual, all gathered into `εside`):**
1. `εside`'s EIGHT non-width §-engine feeders — `εQ` (Q-mix), `εfloor` (mC/10 floor), `εP`/`εB`
   (FrontSync side-event / bulk-arrival masses), `εge3`/`εno3`/`εcpos`/`εsucc` (the four
   phase-gate-failure masses). Each is its own §-engine; the deterministic ones (`εge3`, `εno3`)
   are 0 once start facts propagate, the rest are named whp inputs. (campaign §B-11, §B-12, §B-13)
2. The **post-hour τ-uniformity of the width feeder** (`τ > w·KK`): `goodFrontWidth_whp_at_concrete`
   is concrete for `τ ≤ w·KK`; the post-hour absorbed/already-converged width mode is the one
   surviving §6 width follow-up (NOT a math gap — an engine-rehoming/absorbed-mode task).
   (campaign §B-13 VERDICT)
3. `ClockWeakAssembly.clock_real_faithful_O_log_n_W`'s `q`/`hstep` (per-step gate-escape rate) — the
   §6 drip-only excess-counter one-step bound. (campaign §B-10 RESIDUAL)

**Phase concentration / convergence residuals (the per-phase drain rates `q`/Chernoff numbers):**
4. `RoleSplitConcentration.phase0_stage1_whp` — free `S` + per-step gate-escape `q` (the Lemma-5.1
   floor-concentration Chernoff numbers; with `S=floorGate`, `Sᶜ`-prefix = `∑_τ P(assignableCount<a₀)`).
   (campaign §C-1)
5. `Phase7Convergence.phase7Convergence''` — `hstep` (σ-class-MASS drain rate `q`, Doty Lemma
   7.4/7.5). (campaign §C-7)
6. `Phase8Convergence.phase8Convergence` — `hstep` (minorityU drain rate `q`). (campaign §C-8 land)
7. `Phase5Convergence.phase5Convergence` — `hClosed` (InvClosed Phase5AllWin), `q`/`hstep`
   (unsampledReserveU drain), AND `εConc`/`hConc` (sampled-class concentration). Most residual-laden;
   matches its mid-relay C-5 status. (campaign §C-5)
8. `ConditionalPhaseProgress.timed_phase_progress_real_{tinyClock,bigClock}` — `hfloor :
   ∀ b, AllClockGEpCard p n b → mC ≤ posClockCount p b`, the **Lemma-5.2 clock floor** (a
   ∀-over-class / hwin_all-SHAPE hypothesis). Documented as the single E4-supplied probabilistic
   ingredient, NOT InvClosed-closed. (campaign §E3-6, commit e5fe9973)

**Generic-engine carried parameters (NOT protocol residuals — these are the engine's API, instantiated
per-phase by inputs 3–8):** the `q`/`hstep`/`r`/`δ`/absorption hypotheses of GatedEscape, GatedKillNow,
KernelWindowDrift, ExpectedHitting, OneSidedCancel, CounterTimeout, PhaseConvergenceWeak. These are
correctly generic (`K : Kernel α α [IsMarkovKernel K]`), not smuggled protocol claims.

**Already CLOSED (no residual — do not re-list as TODO):** `Phase10ExpectedTime.phase10_expected_
stabilization'`/`_O_nsq_log`/`_tie` (only `hn`+start); `Phase10Convergence.*` (only `hn`+`hsB`+start,
whp rate internal); `Phase4Convergence.phase4Convergence` (explicit closed rate, no carried `q`).

---

## 7. FLAGS

- **[SHAPE — review, NOT a defect]** `ConditionalPhaseProgress.timed_phase_progress_real_{tiny,big}Clock`
  carry `hfloor : ∀ b ∈ AllClockGEpCard, mC ≤ posClockCount`. This is the hwin_all SHAPE (∀-over-class
  asserting a nontrivial lower bound). It is HONEST so long as E4 discharges it whp (Lemma 5.2's
  probabilistic clock-floor) — and the campaign explicitly frames it that way (not as a
  ∀-reachable-deterministic fact). It would become a smuggle ONLY if a downstream consumer treats it
  as deterministically-true-for-all-reachable. Phase D/F must wire it through a whp discharge, not an
  unconditional assumption. Flagged for Phase-D wiring review.
- **[SHAPE — documented]** `DotyParams.goodFrontWidth_whp_final`'s `heB`/`htB`/`hclimbB` are
  ∀-`T<Tcap` measure bounds (the gated-engine feeders). Honest (bound scales with them, RHS→⊤ if
  they→⊤), documented in §B. No action beyond the standard "discharge the feeders" follow-up.
- **[COVERAGE]** 9 scope files (the recently-edited ones) lack prebuilt oleans, so their `#print
  axioms` was not run; source-grep hygiene is clean. Confirm with `#print axioms` after the next
  remote `lake build`. No evidence of any problem — this is a coverage caveat, not a finding.
- **No overstatements found** in the 12 spot-checked campaign DONE-claims (see §8).

---

## 8. Campaign DONE-claim spot-checks (12) — verdict: NO OVERSTATEMENTS

| campaign claim | code reality |
|---|---|
| E1 ExpectedHitting 13 thms axiom-clean | `expectedHitting_split_geometric`/`_geometric`/`_one_step_q` present, axiom-clean ✓ |
| E2 generic coupon engine 100% closed | `occLevel_le`, `coupon_expectedHitting_le`, `…_uniform_on` present; `coupon_expectedHitting_le` axiom-clean via `OneSidedCancel.crude_…` family ✓ |
| E2-10 invariant-relative engine (`InvClosed`/`PotNonincrOn`) | both present (39 / 31 occ in Phase10ExpectedTime) ✓ |
| E2-13 `activeBCount_drop_prob` delivered | present (9 occ) ✓ |
| §E "SCOPING REFINEMENT: PotNonincrOn FALSE even on all-phase-10" | honest caveat; the cited fix `phase10Transition_preserves_signedContribution` EXISTS at `Analysis/Phase10Backup.lean:684` (public, as claimed) ✓ |
| Phase-10 `O(n² log n)` majority headline | `phase10_expected_stabilization'` carries ONLY `hn`+`S1`; `_O_nsq_log` = `3·n²(1+2 log n)`; both axiom-clean ✓ |
| B-12 `clock_unconditional_concrete` single open input `εside` | matches signature read ✓ |
| B-13 εW free-τ via `εWAt`, 8 feeders carried | `sidePrefix_concrete_width` produces exactly that `sideEps` shape ✓ |
| C-1 `phase0_stage1_whp` = Janson + `t·q + ∑ Sᶜ`, free `S`/`q` | matches signature read ✓ |
| C-7 phase7 `hClosed`+`hmono` PROVED INTERNAL, only `hstep` carried | `phase7Convergence''` delegates to `crude_PhaseConvergenceW` with `invClosed_Inv7Sum`/`potNonincrOn_classMassN` supplied internally ✓ |
| E3-6 timed-phase real corollaries, single input = clock floor `hfloor` | matches `timed_phase_progress_real_bigClock` signature ✓ |
| Phase B retires false `hwin_all` | no scope file consumes `hwin_all` as a hypothesis ✓ |

---

## FINAL VERDICT

The 25-file campaign output is **axiom-clean and sorry-free**: zero `sorry`/`axiom`/`native_decide`/
`admit`/`unsafe` across all files, and every one of 16 spot-checked headline theorems prints exactly
`[propext, Classical.choice, Quot.sound]`. The two in-scope `True := trivial` markers are honest
status anchors, not smuggled content. No vacuous-truth capstone was found — each of the 5 named
capstones bounds a genuinely-satisfiable event with a budget that scales monotonically with its
carried inputs (so a degenerate input enlarges, never collapses, the bound). Cross-file feeders are
consistent (the 9-feeder `sideEps` arity matches across ClockBudgets/WidthPrefixConcrete; `heB`/`htB`
thread correctly; the FALSE `hwin_all` is genuinely retired — no scope file carries it). The campaign
file's honest-verdict sections hold up against the code on all 12 spot-checked claims, including the
non-trivial ones (the `Analysis/Phase10Backup` cross-reference exists; phase7's `hClosed`/`hmono` are
genuinely internal; the Phase-10 expectation carries no probabilistic residual).

The honest open surface for Phase D/F is the EIGHT items in §6: the eight non-width `εside` feeders +
the post-hour width mode + the per-phase drain rates `q`/`hstep` for phases 0/5/7/8 + the Lemma-5.2
clock floor `hfloor`. All are documented in the campaign file. The single SHAPE to watch in Phase-D
wiring is `ConditionalPhaseProgress`'s `hfloor` (the hwin_all-shaped clock floor) — honest as a
whp/E4 input, a defect only if a consumer were to treat it as deterministic-for-all-reachable.
**No smuggling, no overstatement, no undocumented residual detected.** Recommend a confirming
`#print axioms` pass on the 9 not-yet-rebuilt files after the next remote `lake build`.
