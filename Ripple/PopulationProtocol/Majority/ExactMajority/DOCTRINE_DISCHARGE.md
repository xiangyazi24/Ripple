# DOCTRINE — named-field discharge campaign (overnight 2026-06-11)

Goal (one sentence): discharge every named/carried field of the Doty Theorem 3.1
surfaces until the carried set is empty or every survivor has a documented
terminal verdict (genuinely-primitive regime input, or proof-of-falsity).

## Avenues (waves, dependency-ordered)

(a) WAVE A — assembly structure: the three DotyAssembly bridge fields + hcompFail
    + the 11-slot input wiring + the per-rung seeds.
    Terminal: each field closed / produced / reduced to a per-phase named event
    with provenance. ✅ DONE (AssemblyBridges, AssemblyWiring, SeedRungs).

(b) WAVE B — convergence wiring: seed→hTrig handoff + entry residuals;
    per-slot drain rates hstep from the landed rectangles+floors (+persistence);
    landed-endpoint wiring (slot-0 milestone, hside/hεb, hConc, hClosed5/6).
    Terminal: dotyAssembly_concrete' with the narrowest finite carried set;
    per-item wired-or-named-with-provenance. IN FLIGHT (3 agents).

(c) WAVE C — the remaining paper-probability cores:
    Theorem62Paper whp content (per-hour squaring event + HourUnion chain);
    PredecessorLevelsCoPopulated; survival n/5 sharpening (or honest re-budget
    at 14n/75); hBranch checkpoint classification; ceiling snapshots
    (AllBiasedMainBelow/MainHourBelow from the §6 clock chain).
    Terminal: each either proven, or reduced to a DotyRegime-level primitive,
    or proof-of-falsity recorded.

(d) WAVE D — final assembly + audit: instantiate the headline at the fully
    wired assembly; re-run the three-way adversarial audit on the final
    surface; fresh-checkout build; push main + final tag.

Fallbacks: if a wave-C core genuinely resists all engine routes, carry it as a
single named paper-cited fact inside DotyRegime-style bundling and record the
terminal verdict — no silent downgrade, no axiom.

Hard-stops only: external resource, destructive auth, goal complete, all
avenues terminally documented.
